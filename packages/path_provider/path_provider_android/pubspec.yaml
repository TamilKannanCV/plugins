name: path_provider_android
description: A new flutter plugin project.
version: 0.0.1

environment:
  sdk: ">=2.16.2 <3.0.0"
  flutter: ">=2.5.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^1.0.0
  change_app_package_name: ^1.0.0

flutter:
  plugin:
    platforms:
      android:
        package: com.example.path_provider_android
        pluginClass: PathProviderAndroidPlugin
